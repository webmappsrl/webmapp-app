import { CDVPhotoLibraryPipe } from './cdvphotolibrary.pipe';

describe('CDVPhotoLibraryPipe', () => {
  it('create an instance', () => {
    const pipe = new CDVPhotoLibraryPipe(null);
    expect(pipe).toBeTruthy();
  });
});
